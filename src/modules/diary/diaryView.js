export async function renderDiaryView(container) {
  container.innerHTML = `
    <section class="module-diary">
      <p class="placeholder">Модуль «Дневник» — в разработке.</p>
    </section>
  `;
}
