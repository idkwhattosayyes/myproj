export async function renderCalendarView(container) {
  container.innerHTML = `
    <section class="module-calendar">
      <p class="placeholder">Модуль «Календарь» — в разработке.</p>
    </section>
  `;
}
