export async function renderNotesView(container) {
  container.innerHTML = `
    <section class="module-notes">
      <p class="placeholder">Модуль «Заметки» — в разработке.</p>
    </section>
  `;
}
